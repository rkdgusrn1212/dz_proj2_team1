<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="team1.mini2.dz3.model.NoticeDao">

	<resultMap id="noticeMap" type="team1.mini2.dz3.model.NoticeDto">
		<result property="noticeNo" column="notice_no" javaType="java.lang.Integer"
			jdbcType="INTEGER" />
		<result property="noticeTitle" column="notice_title"
			javaType="java.lang.String" jdbcType="VARCHAR" />
		<result property="noticeContent" column="notice_content"
			javaType="java.lang.String" jdbcType="VARCHAR" />
		<result property="noticeRegDate" column="notice_regd"
			javaType="java.util.Date" jdbcType="DATE" />
	</resultMap>
	
	<select id="getList" parameterType="hashmap" resultMap="noticeMap">
		select *
		from (select A.*, ROWNUM r from
		(select * from notice
		order by notice_no) A
		)
		where r >= #{start} and r &lt;= #{end}
	</select>

	<select id="getCount" resultType="Integer">
		select count(*) from notice
	</select>
</mapper>