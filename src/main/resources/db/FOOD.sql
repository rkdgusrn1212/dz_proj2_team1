--보류하겠습니다.
--CREATE TABLE FOOD( -- 음식 테이블
--    FOOD_NO       		NUMBER  	NOT NULL	PRIMARY KEY,	-- 음식 고유번호 (100번부터 시작 1씩 증가)
--    FOOD_NAME   		VARCHAR2(30)	NOT NULL,			-- 음식명
--    FOOD_PRICE    		NUMBER		NOT NULL,			-- 음식가격
--    FOOD_LOCATION 	NUMBER		NOT NULL,			-- 판매점 FK ( RESTAURANT 테이블의 PK 참조 )
--
--     CONSTRAINT 
--     FK_FOOD FOREIGN KEY(FOOD_LOCATION) REFERENCES RESTAURANT(RESTAURANT_NO) ON DELETE CASCADE
--);
--
--CREATE SEQUENCE RESTAURANT_SEQ START WITH 100 INCREMENT BY 1 NOCYCLE;
--SELECT*FROM FOOD;